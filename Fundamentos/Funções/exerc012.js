function marior(a) {
    if (a > b) {
        return a + " é maior que " + b;
    } else if (a < b) {
        return a + " é menor que " + b;
    }
    return a + " é igual a " + b;
}

let a = 30;
let b = 20;

console.log(marior(a, b));